package com.fuzzy.autocity;

import com.badlogic.gdx.utils.Json;

public class WorldObjectLoader {
    public WorldObjectLoader() {
        String[] files = {"buildings.json", "paths.json", "resources.json"};

        for (String file : files) {
            //new Json().fromJson();
        }
    }
}
